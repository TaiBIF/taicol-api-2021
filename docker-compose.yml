version: '3.7'

services:
  web:
    build: .
    container_name: tc-api-container-dev
    image: tc-api-image-dev
    command: /srv/start
    volumes:
      - .:/code
      - ../taicol-volumes/static:/code/conf/static
    ports:
      - 8000:8000
    env_file:
      - .env
    restart: always
